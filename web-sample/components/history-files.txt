    <div className="absolute top-4 right-4 flex gap-2">
          <Sheet onOpenChange={(open) => !open && setSheetView("main")}>
            <SheetTrigger asChild>
              <button className="w-10 h-10 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center text-white">
                <Menu className="w-6 h-6" />
              </button>
            </SheetTrigger>
            <SheetContent
              side="right"
              className="w-[320px] p-0 bg-white border-0"
            >
              <ScrollArea className="h-full">
                <div className="p-6 space-y-4">
                  {sheetView === "main" ? (
                    <div className="space-y-3 mt-6 animate-in fade-in slide-in-from-left-2 duration-200">
                      {/* <SidebarItem icon={SvgShare} label="Share Profile" /> */}
                      <SidebarItem icon={Copy} label="Copy Link" 
                      onClick={handleCopyLink}
                      />
                      <SidebarItem
                        icon={Rss}
                        label="Feeds"
                        onClick={() => handleNavigation("/feeds")}
                      />
                      <SidebarItem
                        icon={Heart}
                        label="Liked"
                        onClick={() => handleNavigation("/home/liked")}
                      />
                      <SidebarItem
                        icon={Users}
                        label="Communities"
                        onClick={() => handleNavigation("/communities")}
                      />
                    </div>
                  ) : (
                    <div className="space-y-4">
                      <button
                        onClick={() => setSheetView("main")}
                        className="flex items-center gap-2 text-gray-700"
                      >
                        <ChevronLeft className="w-5 h-5" />
                        <span className="font-medium">Manage Account</span>
                      </button>
                      <div className="space-y-3 mt-2">
                        <SidebarItem icon={UserCog} label="Account Setting" />
                        <SidebarItem
                          icon={CircleUserRound}
                          label="Profile Setting"
                        />
                        <SidebarItem icon={Lock} label="Privacy & Safety" />
                        <SidebarItem
                          icon={Bell}
                          label="Notifications Setting"
                        />
                      </div>
                    </div>
                  )}
                </div>
              </ScrollArea>
            </SheetContent>
          </Sheet>
        </div>